#include <stdio.h>
int main() {
    char string[200];
    char hello[200];
    scanf("%s",&string);
    scanf("%s",&human);
    printf("You entered : %s and %s\n", string, human);
    return 0;
}